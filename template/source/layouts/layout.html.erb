<%-# Insert under the index.html downloaded from Designmodo -%>

<%-# Replace all data inside HEAD tags with render partials -%>
<%-# = render "partials/head" -%>

<%-# Replace all data inside BODY tags with render partials -%>
<%-# = yield -%>

<%-# Replace all script tags with type javascript with render partials -%>
<%-# = render "partials/js_includes" -%>
